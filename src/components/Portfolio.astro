---
const projects = [
  {
    category: "E-Commerce",
    title: "Luxury Fashion Brand",
    description: "Complete digital transformation with new website, SEO, PPC, and social media campaigns driving 340% sales growth.",
    metrics: [
      { value: "340%", label: "Sales Increase" },
      { value: "4.2x", label: "ROAS" },
      { value: "#1", label: "Google Rankings" }
    ],
    gradient: "linear-gradient(135deg, #667eea 0%, #764ba2 100%)"
  },
  {
    category: "Local Business",
    title: "Multi-Location Restaurant Chain",
    description: "Full-service marketing including website redesign, local SEO, social media, and display ads resulted in massive growth.",
    metrics: [
      { value: "285%", label: "Foot Traffic" },
      { value: "12x", label: "Online Orders" },
      { value: "150+", label: "5-Star Reviews" }
    ],
    gradient: "linear-gradient(135deg, #f093fb 0%, #f5576c 100%)"
  },
  {
    category: "Professional Services",
    title: "Law Firm Marketing",
    description: "Comprehensive strategy with new website, SEO, Google Ads, and content marketing to dominate local search.",
    metrics: [
      { value: "520%", label: "Lead Growth" },
      { value: "-58%", label: "Cost Per Lead" },
      { value: "Page 1", label: "All Keywords" }
    ],
    gradient: "linear-gradient(135deg, #4facfe 0%, #00f2fe 100%)"
  }
];
---

<section id="portfolio" class="portfolio">
  <div class="container">
    <div class="stats-banner">
      <div class="stat-box">
        <span class="stat-big">259%</span>
        <span class="stat-text">Marketing AI Adoption</span>
      </div>
      <div class="stat-box">
        <span class="stat-big">158%</span>
        <span class="stat-text">ROAS Improvement</span>
      </div>
      <div class="stat-box">
        <span class="stat-big">72%</span>
        <span class="stat-text">Cost Reduction</span>
      </div>
    </div>

    <div class="section-header">
      <span class="section-label">Case Studies</span>
      <h2 class="section-title">Real Results from Real Businesses</h2>
      <p class="section-description">
        See how our complete marketing solutions have transformed businesses across industries
      </p>
    </div>
    <div class="portfolio-grid">
      {projects.map((project) => (
        <div class="portfolio-card">
          <div class="portfolio-image" style={`background: ${project.gradient}`}>
            <div class="portfolio-overlay">
              <span class="portfolio-category">{project.category}</span>
            </div>
          </div>
          <div class="portfolio-content">
            <h3 class="portfolio-title">{project.title}</h3>
            <p class="portfolio-description">{project.description}</p>
            <div class="portfolio-metrics">
              {project.metrics.map((metric) => (
                <div class="metric">
                  <span class="metric-value">{metric.value}</span>
                  <span class="metric-label">{metric.label}</span>
                </div>
              ))}
            </div>
            <a href="#" class="portfolio-link">Read Case Study →</a>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  // Portfolio Card 3D Hover Effect
  const portfolioCards = document.querySelectorAll('.portfolio-card');

  portfolioCards.forEach(card => {
    card.addEventListener('mousemove', (e) => {
      const rect = card.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;

      const centerX = rect.width / 2;
      const centerY = rect.height / 2;

      const rotateX = (y - centerY) / 20;
      const rotateY = (centerX - x) / 20;

      (card as HTMLElement).style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateY(-5px)`;
    });

    card.addEventListener('mouseleave', () => {
      (card as HTMLElement).style.transform = 'perspective(1000px) rotateX(0) rotateY(0) translateY(0)';
    });
  });
</script>

<style>
  .portfolio {
    background: var(--bg-dark);
  }

  .stats-banner {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
    padding: 3rem;
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
    border: 1px solid rgba(99, 102, 241, 0.2);
    border-radius: 20px;
  }

  .stat-box {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 1.5rem;
  }

  .stat-big {
    font-size: 3rem;
    font-weight: 900;
    background: var(--gradient-1);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    margin-bottom: 0.5rem;
  }

  .stat-text {
    color: var(--text-secondary);
    font-size: 0.95rem;
    font-weight: 600;
  }

  .portfolio-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }

  .portfolio-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-color);
    border-radius: 16px;
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .portfolio-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    border-color: var(--primary-color);
  }

  .portfolio-image {
    height: 250px;
    position: relative;
    overflow: hidden;
  }

  .portfolio-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.4);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .portfolio-category {
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    padding: 0.5rem 1rem;
    border-radius: 20px;
    color: white;
    font-weight: 600;
    font-size: 0.9rem;
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  .portfolio-content {
    padding: 2rem;
  }

  .portfolio-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .portfolio-description {
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
    line-height: 1.7;
  }

  .portfolio-metrics {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 12px;
    border: 1px solid var(--border-color);
  }

  .metric {
    flex: 1;
    text-align: center;
  }

  .metric-value {
    display: block;
    font-size: 1.8rem;
    font-weight: 800;
    background: var(--gradient-1);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.25rem;
  }

  .metric-label {
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  .portfolio-link {
    color: var(--primary-light);
    text-decoration: none;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    transition: all 0.3s ease;
  }

  .portfolio-link:hover {
    color: var(--primary-color);
    transform: translateX(5px);
  }

  @media (max-width: 1024px) {
    .portfolio-grid {
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    }
  }

  @media (max-width: 768px) {
    .portfolio-grid {
      grid-template-columns: 1fr;
    }

    .portfolio-metrics {
      flex-direction: column;
      gap: 1rem;
    }
  }
</style>
