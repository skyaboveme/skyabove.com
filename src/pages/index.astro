---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Portfolio from '../components/Portfolio.astro';
import Testimonials from '../components/Testimonials.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="Skyabove - Complete Marketing & Development Agency | Marketing, AI, Full-Stack Development"
  description="Your complete marketing and development team. Marketing strategy, content creation, AI optimization, SEO, full-stack development, marketing automation. 3.8x average ROAS. One comprehensive package: $3,500/month."
>
  <Navigation />

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-background">
      <canvas id="gridCanvas" class="grid-canvas"></canvas>
      <div class="gradient-orb orb-1"></div>
      <div class="gradient-orb orb-2"></div>
      <div class="gradient-orb orb-3"></div>
    </div>
    <div class="container">
      <div class="hero-content">
        <h1 class="hero-title">
          <span class="title-line">Complete Marketing & Development Agency</span>
          <span class="title-line"><span class="gradient-text">Marketing • AI • Full-Stack Development</span></span>
        </h1>
        <p class="hero-subtitle">
          Everything you need to grow. Marketing strategy, content creation, AI optimization, SEO, full-stack development, marketing automation—all in one comprehensive package powered by enterprise AI.
        </p>
        <div class="hero-price-card">
          <div class="price-badge">One Comprehensive Package</div>
          <div class="hero-price">
            <span class="price-currency">$</span>
            <span class="price-amount">3,500</span>
            <span class="price-period">/month</span>
          </div>
          <p class="price-commitment">3-month minimum • Month to month after</p>
        </div>
        <div class="hero-stats">
          <div class="stat">
            <div class="stat-number">3.8x</div>
            <div class="stat-label">Average ROAS</div>
          </div>
          <div class="stat">
            <div class="stat-number">259%</div>
            <div class="stat-label">Revenue Growth</div>
          </div>
          <div class="stat">
            <div class="stat-number">24/7</div>
            <div class="stat-label">Marketing Engine</div>
          </div>
        </div>
      </div>
    </div>
    <div class="scroll-indicator">
      <span class="scroll-text">Scroll to explore</span>
      <div class="scroll-arrow"></div>
    </div>
  </section>

  <!-- Comprehensive Package Section -->
  <section id="packages" class="packages">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Everything You Need</span>
        <h2 class="section-title">One Comprehensive Package: $3,500/Month</h2>
        <p class="section-description">
          Marketing, content, AI, SEO, development, and automation—all working together to drive growth
        </p>
      </div>

      <div class="ai-team-banner">
        <div class="banner-icon">🚀💻🎨</div>
        <div class="banner-content">
          <h3 class="banner-title">Your Complete Growth Team: Marketing, Development, AI & Automation</h3>
          <p class="banner-description">
            Not just marketing. Not just development. You get a complete growth team—marketers, developers, designers, content creators, SEO specialists, and AI experts—all working together to scale your business.
          </p>
        </div>
      </div>

      <!-- Single Comprehensive Package -->
      <div class="single-package">
        <div class="package-hero">
          <div class="package-pricing">
            <div class="pricing-badge">⚡ Everything Included</div>
            <div class="price-display">
              <span class="price-currency">$</span>
              <span class="price-number">3,500</span>
              <span class="price-period">/month</span>
            </div>
            <p class="pricing-note">3-month minimum • Month to month after • Cancel anytime</p>
          </div>
        </div>

        <div class="package-services">
          <!-- Marketing Services -->
          <div class="service-category">
            <div class="category-header">
              <div class="category-icon">📈</div>
              <h3>Marketing & Growth</h3>
            </div>
            <ul class="service-list">
              <li>Marketing strategy & positioning</li>
              <li>Paid advertising (Google, Meta, LinkedIn)</li>
              <li>Social media management & growth</li>
              <li>Email & SMS marketing campaigns</li>
              <li>Marketing automation workflows</li>
              <li>Conversion rate optimization</li>
              <li>Analytics & ROAS tracking</li>
              <li>Customer acquisition funnels</li>
            </ul>
          </div>

          <!-- Content Creation -->
          <div class="service-category">
            <div class="category-header">
              <div class="category-icon">✍️</div>
              <h3>Content Creation</h3>
            </div>
            <ul class="service-list">
              <li>Blog posts & articles (SEO-optimized)</li>
              <li>Social media content daily</li>
              <li>Video production & editing</li>
              <li>Graphic design & creative assets</li>
              <li>Email copywriting</li>
              <li>Ad creative & variations</li>
              <li>Product descriptions</li>
              <li>Brand messaging & voice</li>
            </ul>
          </div>

          <!-- AI Optimization -->
          <div class="service-category">
            <div class="category-header">
              <div class="category-icon">🤖</div>
              <h3>AI Optimization</h3>
            </div>
            <ul class="service-list">
              <li>Enterprise AI tools (ChatGPT, Claude, Gemini)</li>
              <li>AI-powered content generation</li>
              <li>Automated workflow optimization</li>
              <li>Predictive analytics & insights</li>
              <li>AI chatbots & customer service</li>
              <li>Intelligent automation systems</li>
              <li>AI image & video generation</li>
              <li>Process automation with AI</li>
            </ul>
          </div>

          <!-- SEO & Optimization -->
          <div class="service-category">
            <div class="category-header">
              <div class="category-icon">🔍</div>
              <h3>SEO & Optimization</h3>
            </div>
            <ul class="service-list">
              <li>Technical SEO optimization</li>
              <li>On-page & off-page SEO</li>
              <li>Keyword research & targeting</li>
              <li>Local SEO & Google Business Profile</li>
              <li>Schema markup & rich snippets</li>
              <li>Core Web Vitals optimization</li>
              <li>Backlink building strategy</li>
              <li>SEO analytics & reporting</li>
            </ul>
          </div>

          <!-- Full-Stack Development -->
          <div class="service-category">
            <div class="category-header">
              <div class="category-icon">💻</div>
              <h3>Full-Stack Development</h3>
            </div>
            <ul class="service-list">
              <li>Custom website development</li>
              <li>Web application development</li>
              <li>E-commerce solutions</li>
              <li>API development & integrations</li>
              <li>Database design & optimization</li>
              <li>Mobile-responsive design</li>
              <li>Performance optimization</li>
              <li>Ongoing maintenance & updates</li>
            </ul>
          </div>

          <!-- Marketing Automation -->
          <div class="service-category">
            <div class="category-header">
              <div class="category-icon">⚙️</div>
              <h3>Marketing Automation</h3>
            </div>
            <ul class="service-list">
              <li>CRM setup & automation</li>
              <li>Email marketing automation</li>
              <li>Lead nurturing sequences</li>
              <li>Sales funnel automation</li>
              <li>Customer journey mapping</li>
              <li>Zapier/Make.com workflows</li>
              <li>Automated reporting dashboards</li>
              <li>Integration management</li>
            </ul>
          </div>
        </div>

        <div class="package-cta">
          <h3>Ready to Transform Your Business?</h3>
          <p>Get everything you need to grow in one comprehensive package</p>
          <div class="cta-buttons">
            <a href="#contact" class="btn btn-primary btn-large">Get Started Today</a>
            <a href="tel:+19517600896" class="btn btn-secondary btn-large">📞 (951) 760-0896</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Marketing Services -->
  <section class="why-ai">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Complete Marketing Services</span>
        <h2 class="section-title">Everything You Need to Dominate Your Market</h2>
        <p class="section-description">
          From strategy to execution, creative to analytics—we handle every aspect of your marketing
        </p>
      </div>

      <div class="ai-tools-grid">
        <div class="ai-tool">
          <div class="tool-logo">🎯</div>
          <h4>Marketing Strategy</h4>
          <p>Positioning & planning</p>
        </div>
        <div class="ai-tool">
          <div class="tool-logo">🎨</div>
          <h4>Creative Production</h4>
          <p>Ads, graphics, branding</p>
        </div>
        <div class="ai-tool">
          <div class="tool-logo">📱</div>
          <h4>Social Media</h4>
          <p>All platforms managed</p>
        </div>
        <div class="ai-tool">
          <div class="tool-logo">🎬</div>
          <h4>Video Production</h4>
          <p>Editing & motion graphics</p>
        </div>
        <div class="ai-tool">
          <div class="tool-logo">💰</div>
          <h4>Paid Advertising</h4>
          <p>Google, Meta, LinkedIn</p>
        </div>
        <div class="ai-tool">
          <div class="tool-logo">✍️</div>
          <h4>Content Marketing</h4>
          <p>Blogs, emails, SEO</p>
        </div>
        <div class="ai-tool">
          <div class="tool-logo">📊</div>
          <h4>Analytics & ROAS</h4>
          <p>Data-driven optimization</p>
        </div>
        <div class="ai-tool">
          <div class="tool-logo">🤖</div>
          <h4>AI-Powered</h4>
          <p>Enterprise AI tools</p>
        </div>
      </div>

      <div class="ai-value-prop">
        <div class="value-item highlight">
          <span class="value-label">One team, one price—everything marketing</span>
          <span class="value-amount">ALL INCLUDED</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Portfolio -->
  <Portfolio />

  <!-- Testimonials -->
  <Testimonials />

  <!-- Results Section -->
  <section class="results">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Proven Marketing Results</span>
        <h2 class="section-title">Real ROI from Real Marketing Campaigns</h2>
      </div>

      <div class="results-grid">
        <div class="result-card">
          <div class="result-stat">3.8x</div>
          <div class="result-label">Average ROAS</div>
          <p class="result-desc">Every dollar spent returns $3.80 in revenue</p>
        </div>

        <div class="result-card">
          <div class="result-stat">259%</div>
          <div class="result-label">Average Revenue Growth</div>
          <p class="result-desc">Year-over-year growth from marketing efforts</p>
        </div>

        <div class="result-card">
          <div class="result-stat">127%</div>
          <div class="result-label">More Qualified Leads</div>
          <p class="result-desc">Higher quality leads that convert to customers</p>
        </div>

        <div class="result-card">
          <div class="result-stat">14 Days</div>
          <div class="result-label">To First Campaign</div>
          <p class="result-desc">Campaigns live and generating results fast</p>
        </div>

        <div class="result-card">
          <div class="result-stat">8.2%</div>
          <div class="result-label">Average Conversion Rate</div>
          <p class="result-desc">Industry-beating conversion optimization</p>
        </div>

        <div class="result-card">
          <div class="result-stat">24/7</div>
          <div class="result-label">Marketing Engine</div>
          <p class="result-desc">Always-on marketing driving continuous growth</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="final-cta">
    <div class="container">
      <div class="cta-content">
        <h2 class="cta-title">Ready to Scale Your Business?</h2>
        <p class="cta-description">
          Get marketing, development, AI, and automation—all in one package at $3,500/month
        </p>
        <div class="cta-price-highlight">
          <div class="cta-price">
            <span class="cta-price-amount">$3,500</span>
            <span class="cta-price-period">/month</span>
          </div>
          <p class="cta-price-note">Everything included • 3-month minimum</p>
        </div>
        <div class="cta-buttons">
          <a href="#contact" class="btn btn-primary btn-large">Get Started Today</a>
          <a href="tel:+19517600896" class="btn btn-secondary btn-large">📞 (951) 760-0896</a>
        </div>
        <p class="cta-contact">
          Available 9 AM - 6 PM PST • <a href="mailto:hello@skyabove.com">hello@skyabove.com</a>
        </p>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contact" class="contact">
    <div class="container">
      <div class="contact-wrapper">
        <div class="contact-info">
          <span class="section-label">Get Started</span>
          <h2 class="section-title">Let's Build Your Growth Engine</h2>
          <p class="contact-description">
            Schedule a free consultation to discuss your goals. We'll show you exactly how our comprehensive package can transform your business.
          </p>
          <div class="contact-details">
            <div class="contact-item">
              <div class="contact-icon">📞</div>
              <div>
                <h4>Phone</h4>
                <p><a href="tel:+19517600896">(951) 760-0896</a></p>
                <p class="hours">Available 9 AM - 6 PM PST</p>
              </div>
            </div>
            <div class="contact-item">
              <div class="contact-icon">📧</div>
              <div>
                <h4>Email</h4>
                <p><a href="mailto:hello@skyabove.com">hello@skyabove.com</a></p>
              </div>
            </div>
          </div>
        </div>
        <div class="contact-form-wrapper">
          <form class="contact-form" id="contactForm">
            <div class="form-group">
              <label for="name">Full Name</label>
              <input type="text" id="name" name="name" required>
            </div>
            <div class="form-group">
              <label for="email">Email Address</label>
              <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
              <label for="phone">Phone Number</label>
              <input type="tel" id="phone" name="phone">
            </div>
            <div class="form-group">
              <label for="company">Company Name (optional)</label>
              <input type="text" id="company" name="company">
            </div>
            <div class="form-group">
              <label for="message">Tell us about your business</label>
              <textarea id="message" name="message" rows="5" required></textarea>
            </div>
            <button type="submit" class="btn btn-primary btn-full">
              Schedule Free Consultation
            </button>
          </form>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  /* Hero Section */
  .hero {
    position: relative;
    min-height: 80vh;
    display: flex;
    align-items: center;
    padding: 8rem 0 4rem;
    overflow: hidden;
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 0;
    background: radial-gradient(ellipse at center, rgba(15, 23, 42, 0.8) 0%, var(--bg-darker) 100%);
  }

  .grid-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.2;
    animation: float 20s infinite ease-in-out;
    z-index: 2;
    pointer-events: none;
  }

  .orb-1 {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, #6366f1 0%, transparent 70%);
    top: -15%;
    left: -15%;
    animation-delay: 0s;
  }

  .orb-2 {
    width: 700px;
    height: 700px;
    background: radial-gradient(circle, #ec4899 0%, transparent 70%);
    top: 20%;
    right: -20%;
    animation-delay: 7s;
  }

  .orb-3 {
    width: 550px;
    height: 550px;
    background: radial-gradient(circle, #14b8a6 0%, transparent 70%);
    bottom: -15%;
    left: 25%;
    animation-delay: 14s;
  }

  @keyframes float {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(30px, -30px) scale(1.1); }
    50% { transform: translate(-20px, 20px) scale(0.9); }
    75% { transform: translate(40px, 10px) scale(1.05); }
  }

  .hero-content {
    position: relative;
    z-index: 10;
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
    animation: fadeInUp 1s ease-out;
  }

  .hero-title {
    font-size: 4.5rem;
    font-weight: 900;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--text-primary);
    letter-spacing: -0.02em;
  }

  .title-line {
    display: block;
    animation: fadeInUp 1s ease-out backwards;
  }

  .title-line:nth-child(1) {
    animation-delay: 0.1s;
  }

  .title-line:nth-child(2) {
    animation-delay: 0.2s;
  }

  .gradient-text {
    background: linear-gradient(135deg, #6366f1, #8b5cf6, #06b6d4);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    position: relative;
    display: inline-block;
  }

  .hero-subtitle {
    font-size: 1.4rem;
    line-height: 1.7;
    color: var(--text-secondary);
    margin-bottom: 3rem;
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .stat {
    text-align: center;
    padding: 2rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-color);
    border-radius: 12px;
  }

  .stat-number {
    display: block;
    font-size: 3rem;
    font-weight: 900;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
  }

  .stat-label {
    font-size: 1rem;
    color: var(--text-secondary);
  }

  .hero-price-card {
    background: rgba(255, 255, 255, 0.05);
    border: 2px solid var(--primary-color);
    border-radius: 20px;
    padding: 2rem 3rem;
    margin: 2.5rem auto;
    max-width: 600px;
  }

  .price-badge {
    text-align: center;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--primary-light);
    margin-bottom: 1rem;
  }

  .hero-price {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .price-currency {
    font-size: 2rem;
    color: var(--primary-light);
  }

  .price-amount {
    font-size: 4rem;
    font-weight: 900;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .price-period {
    font-size: 1.5rem;
    color: var(--text-muted);
  }

  .price-commitment {
    text-align: center;
    font-size: 0.95rem;
    color: var(--text-secondary);
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    animation: fadeInUp 1s ease-out 0.7s backwards;
    z-index: 10;
  }

  .scroll-text {
    font-size: 0.85rem;
    color: var(--text-muted);
    display: block;
    margin-bottom: 0.5rem;
  }

  .scroll-arrow {
    width: 2px;
    height: 30px;
    background: var(--text-muted);
    margin: 0 auto;
    position: relative;
    animation: scrollArrow 2s infinite ease-in-out;
  }

  .scroll-arrow::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 8px;
    height: 8px;
    border-right: 2px solid var(--text-muted);
    border-bottom: 2px solid var(--text-muted);
    transform: translateX(-50%) rotate(45deg);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes scrollArrow {
    0%, 100% {
      transform: translateY(0);
      opacity: 0.5;
    }
    50% {
      transform: translateY(10px);
      opacity: 1;
    }
  }

  /* Packages Section */
  .packages {
    padding: 6rem 0;
    background: var(--bg-darker);
  }

  .ai-team-banner {
    display: flex;
    align-items: center;
    gap: 2rem;
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.15) 0%, rgba(139, 92, 246, 0.15) 100%);
    border: 2px solid rgba(99, 102, 241, 0.3);
    border-radius: 20px;
    padding: 2.5rem 3rem;
    margin: 3rem 0;
    position: relative;
    overflow: hidden;
  }

  .ai-team-banner::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    animation: shine 3s infinite;
  }

  @keyframes shine {
    to {
      left: 100%;
    }
  }

  .banner-icon {
    font-size: 3rem;
    line-height: 1;
    flex-shrink: 0;
  }

  .banner-content {
    flex: 1;
  }

  .banner-title {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--text-primary);
    margin-bottom: 0.75rem;
    background: linear-gradient(135deg, #6366f1, #8b5cf6, #06b6d4);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .banner-description {
    font-size: 1.05rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  .packages-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(450px, 1fr));
    gap: 3rem;
    margin-top: 4rem;
    margin-bottom: 5rem;
  }

  .package-card {
    background: rgba(255, 255, 255, 0.03);
    border: 2px solid var(--border-color);
    border-radius: 24px;
    padding: 3rem;
    transition: all 0.3s ease;
    position: relative;
  }

  .package-card.featured {
    border-color: var(--primary-color);
    box-shadow: 0 20px 60px rgba(99, 102, 241, 0.3);
  }

  .package-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 25px 70px rgba(99, 102, 241, 0.4);
  }

  .package-badge {
    display: inline-block;
    background: rgba(99, 102, 241, 0.15);
    border: 1px solid rgba(99, 102, 241, 0.3);
    color: var(--primary-light);
    padding: 0.5rem 1rem;
    border-radius: 50px;
    font-size: 0.9rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }

  .package-badge.popular {
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    border: none;
    color: white;
  }

  .package-name {
    font-size: 2.5rem;
    font-weight: 900;
    margin-bottom: 0.75rem;
    color: var(--text-primary);
  }

  .package-tagline {
    font-size: 1.1rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  .package-price {
    display: flex;
    align-items: baseline;
    justify-content: center;
    margin-bottom: 0.5rem;
  }

  .currency {
    font-size: 2rem;
    color: var(--primary-light);
  }

  .amount {
    font-size: 4.5rem;
    font-weight: 900;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .period {
    font-size: 1.5rem;
    color: var(--text-muted);
  }

  .package-commitment {
    text-align: center;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    font-size: 0.95rem;
  }

  .package-ideal {
    background: rgba(99, 102, 241, 0.1);
    padding: 1.5rem;
    border-radius: 12px;
    margin-bottom: 2rem;
  }

  .package-ideal strong {
    display: block;
    margin-bottom: 0.75rem;
    color: var(--primary-light);
  }

  .package-ideal ul {
    list-style: none;
  }

  .package-ideal li {
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
    color: var(--text-secondary);
  }

  .package-ideal li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--primary-color);
    font-weight: bold;
  }

  .package-features {
    margin-bottom: 2rem;
  }

  .package-features h4 {
    font-size: 1.1rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .package-features ul {
    list-style: none;
  }

  .package-features li {
    padding: 0.75rem 0;
    padding-left: 2rem;
    position: relative;
    color: var(--text-secondary);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .package-features li:last-child {
    border-bottom: none;
  }

  .package-features li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--accent-color);
    font-weight: bold;
    font-size: 1.2rem;
  }

  .package-result {
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    padding: 1.5rem;
    border-radius: 12px;
    margin-bottom: 2rem;
    text-align: center;
  }

  .package-result strong {
    display: block;
    margin-bottom: 0.5rem;
  }

  .btn-block {
    width: 100%;
    text-align: center;
  }

  /* Comparison Table */
  .comparison-section {
    margin-top: 5rem;
    padding-top: 4rem;
    border-top: 2px solid var(--border-color);
  }

  .comparison-title {
    text-align: center;
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 2rem;
  }

  .comparison-table {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-color);
    border-radius: 16px;
    overflow: hidden;
  }

  .comparison-row {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    border-bottom: 1px solid var(--border-color);
  }

  .comparison-row:last-child {
    border-bottom: none;
  }

  .comparison-row.header {
    background: rgba(99, 102, 241, 0.1);
    font-weight: 700;
  }

  .comparison-cell {
    padding: 1.5rem;
    text-align: center;
    border-right: 1px solid var(--border-color);
  }

  .comparison-cell:last-child {
    border-right: none;
  }

  .comparison-cell.feature-name {
    text-align: left;
    font-weight: 600;
  }

  .comparison-cell.featured {
    background: rgba(99, 102, 241, 0.05);
  }

  /* Single Package Styles */
  .single-package {
    margin-top: 4rem;
  }

  .package-hero {
    text-align: center;
    margin-bottom: 4rem;
  }

  .package-pricing {
    background: rgba(255, 255, 255, 0.03);
    border: 2px solid var(--primary-color);
    border-radius: 24px;
    padding: 3rem;
    max-width: 600px;
    margin: 0 auto;
  }

  .pricing-badge {
    font-size: 1rem;
    font-weight: 700;
    color: var(--primary-light);
    margin-bottom: 1.5rem;
  }

  .price-display {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .price-number {
    font-size: 5rem;
    font-weight: 900;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .pricing-note {
    font-size: 1.05rem;
    color: var(--text-secondary);
  }

  .package-services {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .service-category {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-color);
    border-radius: 16px;
    padding: 2.5rem;
    transition: all 0.3s ease;
  }

  .service-category:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(99, 102, 241, 0.2);
    border-color: var(--primary-color);
  }

  .category-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .category-icon {
    font-size: 2.5rem;
  }

  .category-header h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
  }

  .service-list {
    list-style: none;
  }

  .service-list li {
    padding: 0.75rem 0;
    padding-left: 1.75rem;
    position: relative;
    color: var(--text-secondary);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .service-list li:last-child {
    border-bottom: none;
  }

  .service-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--accent-color);
    font-weight: bold;
    font-size: 1.2rem;
  }

  .package-cta {
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    border-radius: 20px;
    padding: 3rem;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .package-cta h3 {
    font-size: 2rem;
    font-weight: 800;
    margin-bottom: 1rem;
  }

  .package-cta p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    opacity: 0.95;
  }

  /* Why AI Section */
  .why-ai {
    padding: 6rem 0;
    background: var(--bg-dark);
  }

  .ai-tools-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 1.5rem;
    margin: 3rem 0;
  }

  .ai-tool {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 2rem 1.5rem;
    text-align: center;
    transition: all 0.3s ease;
  }

  .ai-tool:hover {
    border-color: var(--primary-color);
    transform: scale(1.05);
  }

  .tool-logo {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .ai-tool h4 {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--text-primary);
  }

  .ai-tool p {
    font-size: 0.9rem;
    color: var(--text-secondary);
  }

  .ai-value-prop {
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    border-radius: 16px;
    padding: 3rem;
    max-width: 700px;
    margin: 0 auto;
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    gap: 2rem;
  }

  .value-item {
    text-align: center;
  }

  .value-label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
  }

  .value-amount {
    display: block;
    font-size: 3rem;
    font-weight: 900;
  }

  .value-item.highlight .value-amount {
    font-size: 4rem;
  }

  /* Results Section */
  .results {
    padding: 6rem 0;
    background: var(--bg-darker);
  }

  .results-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .result-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-color);
    border-radius: 16px;
    padding: 2.5rem;
    text-align: center;
  }

  .result-stat {
    font-size: 4rem;
    font-weight: 900;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.75rem;
  }

  .result-label {
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .result-desc {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Final CTA */
  .final-cta {
    padding: 6rem 0;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
  }

  .cta-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .cta-title {
    font-size: 3rem;
    font-weight: 900;
    margin-bottom: 1rem;
  }

  .cta-description {
    font-size: 1.3rem;
    margin-bottom: 2rem;
    opacity: 0.95;
  }

  .cta-price-highlight {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 2rem;
    margin-bottom: 2.5rem;
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-price {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .cta-price-amount {
    font-size: 3.5rem;
    font-weight: 900;
  }

  .cta-price-period {
    font-size: 1.5rem;
    opacity: 0.8;
  }

  .cta-price-note {
    text-align: center;
    font-size: 1rem;
    opacity: 0.9;
  }

  .cta-buttons {
    display: flex;
    gap: 2rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .btn-large {
    padding: 1.25rem 3rem;
    font-size: 1.2rem;
  }

  .cta-contact {
    font-size: 1.1rem;
    opacity: 0.9;
  }

  .cta-contact a {
    color: white;
    text-decoration: none;
    font-weight: 700;
    border-bottom: 2px solid rgba(255, 255, 255, 0.5);
  }

  .cta-contact a:hover {
    border-bottom-color: white;
  }

  /* Contact Section */
  .contact {
    padding: 6rem 0;
    background: var(--bg-dark);
  }

  .contact-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .contact-details {
    margin-top: 2rem;
  }

  .contact-item {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .contact-icon {
    font-size: 2rem;
  }

  .contact-item h4 {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--text-primary);
  }

  .contact-item p {
    color: var(--text-secondary);
  }

  .contact-item a {
    color: var(--primary-light);
    text-decoration: none;
  }

  .contact-item a:hover {
    text-decoration: underline;
  }

  .hours {
    font-size: 0.9rem;
    margin-top: 0.25rem;
  }

  .contact-form-wrapper {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-color);
    border-radius: 16px;
    padding: 2.5rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 0.875rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    color: var(--text-primary);
    font-size: 1rem;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary-color);
  }

  .btn-full {
    width: 100%;
  }

  /* Responsive */
  @media (max-width: 968px) {
    .hero-title {
      font-size: 3rem;
    }

    .ai-team-banner {
      flex-direction: column;
      text-align: center;
      padding: 2rem;
    }

    .banner-title {
      font-size: 1.3rem;
    }

    .banner-description {
      font-size: 1rem;
    }

    .packages-grid {
      grid-template-columns: 1fr;
    }

    .comparison-row {
      grid-template-columns: 1fr;
    }

    .comparison-cell {
      border-right: none;
      border-bottom: 1px solid var(--border-color);
    }

    .contact-wrapper {
      grid-template-columns: 1fr;
    }

    .cta-buttons {
      flex-direction: column;
    }

    .btn-large {
      width: 100%;
    }

    .ai-value-prop {
      flex-direction: column;
    }
  }
</style>

<script>
  // GA4 Event Tracking
  function trackEvent(eventName: string, params = {}) {
    if (typeof gtag !== 'undefined') {
      gtag('event', eventName, params);
    }
  }

  // Track page view
  trackEvent('page_view', {
    page_title: 'Homepage - Package Selector',
    page_location: window.location.href
  });

  // Form submission
  const form = document.getElementById('contactForm');
  if (form) {
    form.addEventListener('submit', (e) => {
      e.preventDefault();

      const formData = new FormData(form as HTMLFormElement);
      const company = formData.get('company');

      trackEvent('generate_lead', {
        event_category: 'Form',
        event_label: 'Homepage Contact Form',
        company_name: company,
        package_price: 3500
      });

      alert('Thank you! We\'ll be in touch within 24 hours to discuss your needs.');
      form.reset();
    });
  }

  // Track CTA button clicks
  document.querySelectorAll('.btn-primary, .btn-secondary').forEach(button => {
    button.addEventListener('click', (e) => {
      const target = e.currentTarget as HTMLElement;
      const buttonText = target.textContent?.trim() || '';

      trackEvent('cta_click', {
        event_category: 'Engagement',
        event_label: buttonText,
        link_location: 'Homepage'
      });
    });
  });

  // Track phone clicks
  document.querySelectorAll('a[href^="tel:"]').forEach(link => {
    link.addEventListener('click', () => {
      trackEvent('phone_click', {
        event_category: 'Contact',
        event_label: 'Homepage Phone Click'
      });
    });
  });

  // Animated Grid Background
  const canvas = document.getElementById('gridCanvas') as HTMLCanvasElement;
  const ctx = canvas?.getContext('2d');

  if (canvas && ctx) {
    // Set canvas size
    function resizeCanvas() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }
    resizeCanvas();
    window.addEventListener('resize', resizeCanvas);

    // Grid settings
    const gridSize = 50;
    const dotSize = 2;
    const connectionDistance = 150;
    const particles: Array<{x: number, y: number, vx: number, vy: number}> = [];

    // Create particles
    const particleCount = Math.floor((canvas.width * canvas.height) / 15000);
    for (let i = 0; i < particleCount; i++) {
      particles.push({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        vx: (Math.random() - 0.5) * 0.5,
        vy: (Math.random() - 0.5) * 0.5
      });
    }

    // Mouse position
    let mouseX = canvas.width / 2;
    let mouseY = canvas.height / 2;

    canvas.addEventListener('mousemove', (e) => {
      const rect = canvas.getBoundingClientRect();
      mouseX = e.clientX - rect.left;
      mouseY = e.clientY - rect.top;
    });

    function drawGrid() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      // Draw grid dots
      ctx.fillStyle = 'rgba(99, 102, 241, 0.15)';
      for (let x = 0; x < canvas.width; x += gridSize) {
        for (let y = 0; y < canvas.height; y += gridSize) {
          const distToMouse = Math.hypot(mouseX - x, mouseY - y);
          const opacity = Math.max(0.05, Math.min(0.3, 1 - distToMouse / 300));
          ctx.globalAlpha = opacity;
          ctx.beginPath();
          ctx.arc(x, y, dotSize, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      ctx.globalAlpha = 1;

      // Update and draw particles
      particles.forEach((particle, i) => {
        // Update position
        particle.x += particle.vx;
        particle.y += particle.vy;

        // Bounce off edges
        if (particle.x < 0 || particle.x > canvas.width) particle.vx *= -1;
        if (particle.y < 0 || particle.y > canvas.height) particle.vy *= -1;

        // Draw particle
        const distToMouse = Math.hypot(mouseX - particle.x, mouseY - particle.y);
        const size = Math.max(2, Math.min(5, 5 - distToMouse / 200));

        ctx.fillStyle = 'rgba(99, 102, 241, 0.8)';
        ctx.beginPath();
        ctx.arc(particle.x, particle.y, size, 0, Math.PI * 2);
        ctx.fill();

        // Draw connections
        particles.slice(i + 1).forEach(otherParticle => {
          const dist = Math.hypot(particle.x - otherParticle.x, particle.y - otherParticle.y);
          if (dist < connectionDistance) {
            const opacity = (1 - dist / connectionDistance) * 0.3;
            ctx.strokeStyle = `rgba(99, 102, 241, ${opacity})`;
            ctx.lineWidth = 1;
            ctx.beginPath();
            ctx.moveTo(particle.x, particle.y);
            ctx.lineTo(otherParticle.x, otherParticle.y);
            ctx.stroke();
          }
        });
      });

      requestAnimationFrame(drawGrid);
    }

    drawGrid();
  }

  // Animated Counters for Hero Stats
  const statsObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const stats = entry.target.querySelectorAll('.stat-number');
        stats.forEach(stat => {
          const text = stat.textContent || '';

          if (text.includes('%')) {
            const target = parseInt(text.replace('%', ''));
            let current = 0;
            const timer = setInterval(() => {
              if (current >= target) {
                stat.textContent = target + '%';
                clearInterval(timer);
              } else {
                current += Math.ceil(target / 50);
                stat.textContent = Math.min(current, target) + '%';
              }
            }, 30);
          } else {
            // For plain numbers like 127
            const target = parseInt(text);
            let current = 0;
            const increment = target / 50;
            const timer = setInterval(() => {
              current += increment;
              if (current >= target) {
                stat.textContent = target.toString();
                clearInterval(timer);
              } else {
                stat.textContent = Math.floor(current).toString();
              }
            }, 30);
          }
        });
        statsObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.5 });

  const heroStats = document.querySelector('.hero-stats');
  if (heroStats) {
    statsObserver.observe(heroStats);
  }
</script>
